% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/profile.R
\name{hill_number}
\alias{hill_number}
\title{Hill number of order q}
\usage{
hill_number(p, q)
}
\arguments{
\item{p}{numeric vector of non-negative weights summing to (roughly) 1.
Zeros are allowed and ignored.}

\item{q}{non-negative numeric scalar or vector, the order. \code{q = Inf} is
allowed.}
}
\value{
Numeric scalar (if \code{length(q) == 1}) or numeric vector with
\code{length(q)} entries.
}
\description{
Computes the Hill number (true diversity) of order \code{q} for a single
composition vector. Handles the limiting cases \code{q = 0} (richness),
\code{q = 1} (exponential of Shannon entropy), and \code{q = Inf} (\code{1 / max(p)})
explicitly. Vectorized over \code{q}: pass a vector and get back a vector.
}
\examples{
p <- c(0.4, 0.3, 0.2, 0.1)
hill_number(p, 0)      # 4 nonzero parties
hill_number(p, 1)      # exp(Shannon)
hill_number(p, 2)      # ENP = 1 / sum(p^2)
hill_number(p, Inf)    # 1 / max(p)
hill_number(p, c(0, 1, 2, Inf))
}
\seealso{
\code{\link[=diversity_profile]{diversity_profile()}}, \code{\link[=enp]{enp()}}, \code{\link[=hhi]{hhi()}}.
}
