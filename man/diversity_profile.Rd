% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/profile.R
\name{diversity_profile}
\alias{diversity_profile}
\title{Diversity profile}
\usage{
diversity_profile(
  x,
  q = seq(0, 5, by = 0.05),
  anchors = c(0, 1, 2, 5, Inf),
  id = NULL
)
}
\arguments{
\item{x}{numeric vector, matrix, or data.frame of non-negative shares.
Multiple rows compute one profile per row.}

\item{q}{numeric vector of orders at which to evaluate D(q). Default is
a fine grid \code{seq(0, 5, 0.05)}. \code{Inf} is allowed.}

\item{anchors}{numeric vector of "highlight" q values to store as
anchors (useful for plotting). Default \code{c(0, 1, 2, 5, Inf)}.}

\item{id}{optional character vector of row labels.}
}
\value{
An S3 \code{"diversity_profile"} object — a long data.frame with
columns \code{id}, \code{q}, \code{D}, plus attributes \code{q} (grid), \code{anchors},
\code{n_parties}, and \code{wide} (the id-by-q matrix).
}
\description{
Computes the full diversity profile D(q) over a grid of q values for one
or many compositions. Returns an S3 object suitable for \code{\link[ggplot2:autoplot]{ggplot2::autoplot()}},
\code{\link[=plot]{plot()}}, and subset extraction via \code{[}.
}
\examples{
p <- c(0.4, 0.3, 0.2, 0.1)
dp <- diversity_profile(p)
print(dp)
plot(dp)
}
