% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/input.R
\name{as_composition}
\alias{as_composition}
\title{Coerce input to a composition matrix}
\usage{
as_composition(x, id = NULL, tol = 1e-08)
}
\arguments{
\item{x}{numeric vector, matrix, or data.frame of non-negative values.}

\item{id}{optional character vector of length \code{nrow} (after coercion) to
label rows. If \code{NULL}, rownames / names on \code{x} are used.}

\item{tol}{tolerance for "already normalized". Default \code{1e-8}.}
}
\value{
A numeric matrix with \code{nrow(x)} rows and \code{ncol(x)} columns, each
row summing to 1. \code{rownames()} carry the row labels.
}
\description{
Internal helper that normalizes the three supported input shapes into a
numeric matrix of compositions (rows = systems, cols = parties), each row
summing to 1. Exported for power users who want the same validation
elsewhere in their own pipelines.
}
\details{
\itemize{
\item \code{numeric} vector: a single composition. Returned as a 1-row matrix.
\item \code{matrix}: rows are systems, columns are parties. Rownames preserved as
ids when available.
\item \code{data.frame}: numeric columns are used; non-numeric columns are dropped
with a message the first time. The \code{id} argument overrides rownames for
row labels.
}

Rows are rescaled to sum to 1 with a \code{warning()} when the original sum
deviates from 1 by more than \code{tol}. Rows that sum to zero or contain
negative entries raise an error (they are not compositions).
}
