% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parlgov.R
\name{parlgov_seat_shares}
\alias{parlgov_seat_shares}
\title{Reshape ParlGov output to a seat-share matrix}
\usage{
parlgov_seat_shares(
  parlgov,
  countries = NULL,
  years = NULL,
  type = "parliament",
  min_share = 0
)
}
\arguments{
\item{parlgov}{data.frame returned by \code{\link[=fetch_parlgov]{fetch_parlgov()}}.}

\item{countries}{optional character vector of country names to keep.}

\item{years}{optional integer vector; if supplied, only elections whose
year lies in the vector are kept.}

\item{type}{election type to keep. Default \code{"parliament"}.}

\item{min_share}{drop parties whose share is below this threshold
(keeps a sensible column count). Default \code{0} (keep all).}
}
\value{
A numeric matrix (rows = \code{country_year}, cols = party names).
Attributes \code{country} and \code{year} are attached for convenience.
}
\description{
Takes the long ParlGov \code{view_election} output and pivots it to a wide
matrix (one row per country-year election, one column per party) of
seat shares in [0, 1]. Handy as direct input to \code{\link[=diversity_profile]{diversity_profile()}}
or \code{\link[=alpha_beta_gamma]{alpha_beta_gamma()}}.
}
