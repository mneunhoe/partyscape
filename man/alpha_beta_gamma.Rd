% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/beta.R
\name{alpha_beta_gamma}
\alias{alpha_beta_gamma}
\title{Alpha, beta, and gamma diversity (Jost 2007)}
\usage{
alpha_beta_gamma(
  mat,
  q = c(0, 1, 2, 5),
  w = NULL,
  years = NULL,
  collapse = !is.null(years)
)
}
\arguments{
\item{mat}{numeric matrix of non-negative shares (rows = time points /
systems; cols = parties). Accepts anything \code{\link[=as_composition]{as_composition()}} accepts.}

\item{q}{numeric vector of orders. Default \code{c(0, 1, 2, 5)}.}

\item{w}{optional weights (numeric, \code{length = T}, summing to 1). If \code{NULL},
equal weights are used — or term lengths when \code{collapse = TRUE} and
\code{years} are supplied.}

\item{years}{optional integer vector of election years aligned with \code{mat}.
Enables collapsing identical consecutive rows.}

\item{collapse}{logical; defaults to \code{!is.null(years)}.}
}
\value{
S3 \code{"beta_diversity"} object — data.frame with columns \code{q},
\code{alpha}, \code{beta}, \code{gamma}, \code{T}. Attributes carry weights and collapse info.
}
\description{
Multiplicative decomposition of Hill-number diversity across rows of a
composition matrix. Alpha is the generalized mean of order \code{(1 - q)} of
per-row Hill numbers (Jost 2007, Eqs. 11a–11b), gamma is the Hill number
of the (possibly weighted) pooled composition, and beta is \code{gamma / alpha}.
beta is bounded in [1, T] and reads as the "effective number of
structurally distinct compositions" across the T rows. Vectorized over
\code{q}: one call returns a full beta profile.
}
\details{
When \code{years} are supplied (and \code{collapse = TRUE}), consecutive identical
rows are collapsed via \code{\link[=collapse_to_elections]{collapse_to_elections()}} so each unique
composition is counted once; weights default to term lengths so
long-lived elections do not dominate.
}
\examples{
m <- rbind(c(0.5, 0.3, 0.2),
           c(0.4, 0.3, 0.3),
           c(0.5, 0.3, 0.2))
alpha_beta_gamma(m, q = c(0, 1, 2))
}
