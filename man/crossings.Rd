% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/crossings.R
\name{crossings}
\alias{crossings}
\title{Diversity-profile crossings}
\usage{
crossings(
  x,
  y = NULL,
  q = seq(0, 5, by = 0.05),
  method = c("interp", "signchange"),
  tol = 1e-12
)
}
\arguments{
\item{x}{one of:
\itemize{
\item a numeric vector (requires non-null \code{y}),
\item a numeric matrix with rows = systems,
\item a \link{diversity_profile} object.
}}

\item{y}{optional second composition or profile. If supplied and \code{x} is a
single composition / profile, a single pair is scored. Ignored when
\code{x} is itself multi-row.}

\item{q}{grid of q values at which to evaluate profiles (when \code{x}/\code{y} are
compositions rather than profiles). Default \code{seq(0, 5, 0.05)}.}

\item{method}{one of \code{"interp"} or \code{"signchange"}.}

\item{tol}{numeric tolerance for equality to zero.}
}
\value{
S3 \code{"crossings"} list with elements:
\itemize{
\item \code{pairs} — data.frame with columns \code{i}, \code{j}, \code{id_i}, \code{id_j},
\code{n_crossings}, \code{first_q}.
\item \code{locations} — named list of numeric q* vectors, one per pair.
\item \code{profiles} — the underlying \link{diversity_profile} (for plotting).
}
}
\description{
Detects q values at which two diversity profiles D_a(q) and D_b(q) change
order — the central notion of "structural incomparability" in the paper.
Two detection strategies are available.
}
\details{
\itemize{
\item \code{"interp"}: linear interpolation between grid points where
\code{D_a(q) - D_b(q)} changes sign. Precise crossing location; sensitive to
noise in the grid.
\item \code{"signchange"}: count sign changes of the non-zero difference sequence.
Cheap and robust; reports the first crossing's grid location rather than
an interpolated value.
}
}
\examples{
a <- c(0.45, 0.30, 0.15, 0.10)
b <- c(0.40, 0.35, 0.15, 0.10)
crossings(a, b)

mat <- rbind(a = a, b = b, c = c(0.7, 0.2, 0.1, 0))
crossings(mat)
}
